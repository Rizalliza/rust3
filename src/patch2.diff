 (cd "$(git rev-parse --show-toplevel)" && git apply --3way <<'EOF' 
diff --git a/src/main.rs b/src/main.rs
index cf589456b4e9963ca5a4af8c78b172efe11450d1..7936d334e523c6b278cde796208cb11e10945c52 100644
--- a/src/main.rs
+++ b/src/main.rs
@@ -1,31 +1,34 @@
 mod bot;
 mod config;
 mod constants;
 mod dex;
+mod metrics;
+mod paper_trading;
 mod pools;
 mod refresh;
+mod signer;
 mod transaction;
 
 use clap::{App, Arg};
 use tracing::{info, Level};
 use tracing_subscriber::FmtSubscriber;
 
 #[tokio::main]
 async fn main() -> anyhow::Result<()> {
     let subscriber = FmtSubscriber::builder()
         .with_max_level(Level::INFO)
         .finish();
     tracing::subscriber::set_global_default(subscriber)
         .expect("Failed to set global default subscriber");
 
     info!("Starting Solana Onchain Bot");
 
     let matches = App::new("Solana Onchain Arbitrage Bot")
         .version("0.1.0")
         .author("Cetipo")
         .about("A simplified Solana onchain arbitrage bot")
         .arg(
             Arg::with_name("config")
                 .short('c')
                 .long("config")
                 .value_name("FILE")
 
EOF
)